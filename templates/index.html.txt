<!DOCTYPE html>
<html>
<head>
    <title>Recettes compatibles</title>
</head>
<body>
    <h1>Choisissez les athlètes présents</h1>
    <form method="POST">
        {% for nom in athletes %}
            <label>
                <input type="checkbox" name="athletes" value="{{ nom }}"
                    {% if nom in selected %}checked{% endif %}> {{ nom }}
            </label><br>
        {% endfor %}
        <br>
        <button type="submit">Voir les recettes compatibles</button>
    </form>

    {% if result %}
        <h2>Recettes compatibles :</h2>
        <ul>
            {% for recette in result %}
                <li>{{ recette }}</li>
            {% endfor %}
        </ul>
    {% elif selected %}
        <p>Aucune recette compatible.</p>
    {% endif %}
</body>
</html>